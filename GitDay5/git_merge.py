#use command - git branch (new branch name for creation)
# git checkout branch---------- it means switching to other branch
#now write the code
#Slicing with a Step --slice step cannot be zero
#The default step is 1.Python 3 also allows us to slice a string by defining a step through which we can skip characters in the string.

my_string= "This is python programming"
print(my_string[0:2])
print(my_string[3:7:1])
print(my_string[4:6:1])

# now write in terminal git commit -m "developerA story complete"
#now write in terminal- git checkout master
#now delete developerA branch --- git branch -d developerA
#now push to repository using --- git push origin master
